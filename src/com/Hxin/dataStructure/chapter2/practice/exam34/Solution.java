package com.Hxin.dataStructure.chapter2.practice.exam34;

/**
 * @author Hxin
 * @version 1.0
 * @since 2022/4/11
 */
public class Solution {
    /*
    (a) Use one iterator p which initially at the start of the list.Advance p 'x' step at a time ,if p reaches the end
     there is no cycle. x can set a suitable val.
     */

    /*
    (b)Use two iterators p and q, both initially at the start of the list. Advance p one step at a time,
     and q two steps at a time. If q reaches the end there is no cycle; otherwise, p and q will eventually catch up to
     each other in the middle of the cycle.
     */
}
